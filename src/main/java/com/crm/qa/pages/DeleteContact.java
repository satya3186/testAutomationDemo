package com.crm.qa.pages;

import com.crm.qa.base.TestBase;

public class DeleteContact extends TestBase {
    
    // Now that Contact is created, we need to delete the contact
    //Generate a page class construct here to delete the contact
    // use PageFactory class to initialize the web elements
    // Write a method to delete the contact and return the name of the contact deleted
    
    
}
